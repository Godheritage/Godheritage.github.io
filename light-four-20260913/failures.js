const $=id=>document.getElementById(id);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const kinds={collision:'已记录接触／几何失败',timeout:'超时，尚未测完',passed:'本段物理试算通过',static:'静态原形',unmeasured:'未覆盖或提前中止的验证区间',incomplete:'正式流程未完成'};
function entry(row){return `<div class="failure-entry ${esc(row.kind??'timeout')}"><div class="failure-entry-top"><span class="failure-kind">${esc(kinds[row.kind]??row.kind)}</span>${row.time_s!==null&&row.time_s!==undefined?`<span class="failure-time">t = ${Number(row.time_s).toFixed(4).replace(/0+$/,'').replace(/\.$/,'')} s</span>`:''}</div><b>${esc(row.title)}</b><p>${esc(row.detail)}</p>${row.not_proven?`<p class="failure-gap">尚缺：${esc(row.not_proven)}</p>`:''}${row.locate_track?`<button class="failure-locate" data-track="${esc(row.locate_track)}" data-time="${Number(row.time_s)}" data-bodies="${esc((row.body_ids??[]).join('|'))}">${row.body_ids?.length?'定位并标出涉及零件':'定位到记录末点'} →</button>`:''}${row.source_url?`<a class="failure-source" href="${esc(row.source_url)}" target="_blank">查看对应证据 ↗</a>`:''}</div>`}
export function renderTrackFailure(c,t){const rows=c.failure_display?.tracks?.[t.id]??[];$('failure-panel').innerHTML=`<div class="failure-heading"><h2>${t.type==='static'?'当前装配与未完成事项':'这段哪里没过'}</h2><span>与所选记录绑定</span></div>${rows.length?rows.map(entry).join(''):'<p>此记录没有可定位的失败事件；不补造位置。</p>'}<p class="failure-key">橙色表示未完成；红色表示已有失败证据。定位高亮标出整件，不表示接触面积。</p><button id="failure-clear" class="failure-clear" hidden>清除零件高亮</button>`}
export function failureTasks(c){return (c.failure_display?.tests??[]).map(entry).join('')}
